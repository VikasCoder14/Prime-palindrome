def ispelindrome(num):
  s = str(num)
  return s == s[::-1]

def isprime(n):
  for i in range(2,n):
    if (n%i) == 0:
      return False
  return True

n = int(input("Enter n: "))

i = 0
j = 1

while i < n:
  while True:
    j += 1
    if ispelindrome(j) and isprime(j):
      i += 1
      break
j
